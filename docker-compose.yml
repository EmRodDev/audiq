version: "3.9"

services:
  audiq:
    build: .
    image: audiq:latest
    container_name: audiq
    restart: unless-stopped
    env_file:
      - .env
    user: "root"
    working_dir: /usr/src/app
